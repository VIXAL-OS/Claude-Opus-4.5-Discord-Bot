discord.py>=2.3.0
anthropic>=0.50.0
openai>=1.40.0
tavily-python>=0.5.0
python-dotenv>=1.0.0
aiohttp>=3.9.0
matplotlib>=3.7.0
pillow>=10.0.0  # PIL.Image — composites the per-equation LaTeX PNGs into one attachment. Also a matplotlib dep, but pinned explicitly so it can't silently drop out.
beautifulsoup4>=4.12.0  # bookclub mode HTML parsing (AO3 fetcher + !load_text HTML attachments)
pypdf>=4.0.0  # bookclub mode PDF parsing (!load_text .pdf attachments); import-guarded — bot starts fine without it

# OPTIONAL — only for the Gemini `vertex` backend (Phase 2, lab/enterprise tier:
# data residency / IAM / SLA). Lazy-imported, so the bot runs fine without it;
# install + configure GCP ADC (GOOGLE_APPLICATION_CREDENTIALS) only if you set
# providers.gemini.backend = "vertex". The default developer_api backend needs
# nothing here.
# google-cloud-aiplatform>=1.71.0
